RewriteEngine On

# Allow extensionless access to PHP endpoints such as /api/licenses/validate
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^(.+)$ $1.php [L]
